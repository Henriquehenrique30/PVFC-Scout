
// Serviço de integração externa desativado por solicitação do usuário.
// Todos os dados são gerenciados manualmente via App.tsx e LocalStorage.

export const fetchPlayersFromSheet = async (): Promise<any[]> => {
  return [];
};
